import styled from 'styled-components'

const Container = styled.div`
  height: 3200px;
`

const Modal = styled.div`
  z-index: 10;
  position: fixed;
  top: 0;
`

export const Styled = {
  Container,
  Modal,
}
