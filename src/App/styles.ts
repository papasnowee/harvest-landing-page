import styled from 'styled-components'

const Container = styled.div`
  position: relative;
`

const Modal = styled.div`
  z-index: 10;
  position: fixed;
  top: 0;
`

export const Styled = {
  Container,
  Modal,
}
